export const PRIVACY_NOTICE =
  "Non-secret notebooks and notes are reachable by default via agent tools. Secret notebooks are excluded globally. Active and attached notes still seed prompt context. Vault RAG adds bounded snippets only. Bypass permissions applies non-delete proposals without review; deletions still require ChangeReview. Retrieved content is treated as untrusted data. API keys never enter this sidebar or chat files.";
